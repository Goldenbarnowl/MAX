version: '3.9'

services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bot_container
    restart: always
    volumes:
      - .:/app
    command: ["python3", "main.py"]

